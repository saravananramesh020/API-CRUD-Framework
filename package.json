{
  "name": "playwright-api-crud-assignment",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:regression": "playwright test tests/specs/regression",
    "test:regression:allure": "playwright test tests/specs/regression --reporter=list,allure-playwright",
    "allure:generate": "allure generate allure-results --clean -o allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
